import { ExamResultFilterPipe } from './exam-result-filter.pipe';

describe('ExamResultFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExamResultFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
